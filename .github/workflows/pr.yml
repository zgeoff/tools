name: PR

on:
  pull_request:
    branches: [main]

concurrency:
  group: pr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pr:
    permissions:
      contents: read
      pull-requests: write
    uses: ./.github/workflows/bun-checks.yml
    with:
      scripts: check:sync format:check lint:type-aware typecheck test
