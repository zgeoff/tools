{
  "name": "tools",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "typescript": "7.0.1-rc",
      "@types/bun": "1.3.14",
      "@tsconfig/strictest": "2.0.8",
      "@stylistic/eslint-plugin": "5.10.0",
      "jest-extended": "7.0.0",
      "jest-matcher-utils": "30.4.1",
      "lefthook": "2.1.9",
      "oxfmt": "0.56.0",
      "oxlint": "1.71.0",
      "oxlint-tsgolint": "0.23.0"
    }
  },
  "type": "module",
  "scripts": {
    "prepare": "lefthook install --force || true",
    "build:agents": "bash scripts/build-agents-md.sh agents/shared.md agents/project.md AGENTS.md",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "build": "bun run --filter '*' build",
    "lint": "oxlint --report-unused-disable-directives-severity error",
    "lint:fix": "oxlint --fix",
    "lint:type-aware": "oxlint --type-aware --type-check --report-unused-disable-directives-severity error",
    "format": "oxfmt . && bun packages/format-codemod/src/cli.ts --quiet .",
    "format:check": "oxfmt --check . && bun packages/format-codemod/src/cli.ts --check --quiet ."
  },
  "devDependencies": {
    "@tsconfig/strictest": "catalog:",
    "@types/bun": "catalog:",
    "@zgeoff/bun-test-extended": "workspace:*",
    "@zgeoff/format-codemod": "workspace:*",
    "@zgeoff/oxlint-config": "workspace:*",
    "lefthook": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:"
  }
}
