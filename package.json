{
  "name": "tools",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "typescript": "7.0.1-rc",
      "@types/bun": "1.3.14",
      "@tsconfig/strictest": "2.0.8",
      "@stylistic/eslint-plugin": "5.10.0",
      "jest-extended": "7.0.0",
      "jest-matcher-utils": "30.4.1",
      "lefthook": "2.1.9",
      "oxfmt": "0.56.0",
      "oxlint": "1.71.0",
      "oxlint-tsgolint": "0.23.0"
    }
  },
  "type": "module",
  "scripts": {
    "prepare": "lefthook install --force || true",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "build": "bun run --filter '*' build",
    "lint": "oxlint --report-unused-disable-directives-severity error",
    "lint:fix": "oxlint --fix",
    "lint:type-aware": "oxlint --type-aware --type-check --report-unused-disable-directives-severity error",
    "format": "oxfmt . && format-codemod --quiet .",
    "format:check": "oxfmt --check . && format-codemod --check --quiet ."
  },
  "devDependencies": {
    "@stylistic/eslint-plugin": "catalog:",
    "@tsconfig/strictest": "catalog:",
    "@types/bun": "catalog:",
    "@zgeoff/bun-test-extended": "workspace:*",
    "@zgeoff/format-codemod": "workspace:*",
    "lefthook": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:"
  }
}
